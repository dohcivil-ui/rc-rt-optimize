{
  "name": "rcopt-api",
  "version": "0.1.0",
  "description": "REST API wrapper for the rc-rt-optimize backend engine",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/health.test.js && node test/optimize.test.js && node test/optimize.hca.test.js && node test/verification.test.js && node test/statistics.test.js && node test/parseInput.test.js && node test/parseInput.claude.test.js && node test/explainResult.claude.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.0",
    "express": "^4.19.0"
  }
}
